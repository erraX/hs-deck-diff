import React from 'react';
import { Rarity } from '../constants/Rarity';
export interface CardProps {
    id: string;
    name: string;
    cost: number;
    rarity: Rarity;
}

const Card = () => <div></div>;

export default Card;
